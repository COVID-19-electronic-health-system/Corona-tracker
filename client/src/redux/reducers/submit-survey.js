// import { SUBMIT_SURVEY } from '../actions/submit-survey';

// const initialState = {
//   isSubmitted: null,
// };

// const submitSurveyReducer = (state = initialState, action) => {
//   switch (action.type) {
//     case SUBMIT_SURVEY:
//       return { isSubmitted: true };
//     default:
//       return state;
//   }
// };

// export default submitSurveyReducer;
