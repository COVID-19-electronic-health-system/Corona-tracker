language: node_js

node_js:
  "stable"
  
cache:
  directories:
   node_modules

before_install:
  npm -g install yarn
  yarn
  cd client
  
before_script:
  yarn

script:
  yarn test
  yarn run build
